<%= form_for :session, url: '/login' do |f| %>
	
	<%= f.label "E-Mail" %><br>
	<%= f.text_field :email %><br>

	<%= f.label "Password" %><br>
	<%= f.password_field :password %><br>

	<%= f.submit "Login" %>

<% end %>